# Idea Forge

Small client-side toy app for quickly generating project ideas and names.

## Features

- Generate random project ideas and names (10 ideas × 10 names = 100 combinations)
- Press Enter or click "Generate Both" button to generate both instantly
- **Smooth animations** when generating results
- Save current idea+name to local history (persisted in `localStorage`)
- Copy to clipboard — click 📋 on any saved item for quick sharing
- **Search/filter** saved ideas by keyword or name
- **Live counter** showing total saved ideas
- Export and import history as JSON for backup/restore
- Delete individual history items or clear all history
- **Help modal** with keyboard shortcuts (press `?`)
- Fully responsive and keyboard accessible

## Run

Open `index.html` in a browser (no build step required).

## Keyboard Shortcuts

- **Enter** — Generate both idea and name
- **?** — Toggle help panel with shortcuts and tips

## Notes & next improvements

- Consider adding tags for ideas, filtering, and search
- Allow editing saved entries
- Sync to a remote store or integrate with a backend
